interface:
  display_name: "Dev Buddy"
  short_description: "Safely inspect managed servers and relational databases"
  default_prompt: "Use $dev-buddy to inspect managed infrastructure or execute a narrowly scoped, policy-filtered database operation."
